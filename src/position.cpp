#include "position.h"

Position::Position(int row, int column) // Constructor initializes row and column
{
    this->row = row;       // Set the row member variable to the provided row value
    this->column = column; // Set the column member variable to the provided column value
};